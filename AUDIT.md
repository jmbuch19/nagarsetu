# AUDIT.md — Jay Hatkesh (Nagarsetu) Build & Security Log

Append an entry whenever a unit of work is completed. Run the security checklist
before any wider release and re-run it after touching auth, data, or payments.

---

## Security checklist (Supabase + Next.js + Expo)

**Data access**
- [ ] RLS enabled on **every** table (no table left open by default)
- [ ] Policies tested: a member cannot read another member's gated contact details
- [ ] Aggregate intelligence exposed via read-only views only (no row leakage)
- [ ] Admin/editor powers gated by role, server-side

**Auth**
- [ ] Phone OTP rate-limited (prevent OTP abuse/enumeration)
- [ ] Session handling correct on web + mobile; no token leakage
- [ ] Role checks enforced server-side, never trusted from client

**Input & endpoints**
- [ ] All inputs validated/sanitised server-side
- [ ] Public/listing/inquiry endpoints rate-limited
- [ ] File uploads (photos) type/size-checked; stored in scoped Storage buckets
- [ ] Cron/scheduler routes protected by `CRON_SECRET` (reject unauthenticated triggers)

**Money (listing fee only)**
- [ ] Payment verified **server-side** against the gateway (never trust client success)
- [ ] No code path moves member-to-member money through the app
- [ ] No commission logic exists anywhere

**Connector integrity**
- [ ] Connector disclaimer present on every transaction-implying surface
- [ ] Availability is soft status only — no hard lock / no booking guarantee in code
- [ ] App does not store or adjudicate member-to-member payment outcomes

**Secrets & config**
- [ ] All secrets in env, none committed
- [ ] Supabase service-role key never shipped to client
- [ ] Claude / gateway / WhatsApp keys server-only

**Localisation**
- [ ] Gujarati glyphs render correctly across app screens
- [ ] Gujarati fonts embedded in the rendered magazine PDF

---

## Build log
*(append newest at top: date — area — what changed — checks run)*

- 2026-05-29 — Brand/PWA — Wired brand identity (founder request). Generated a brand icon set from the supplied emblem (the ring-of-people mark, cropped from the 1024 app-icon source): `favicon.ico` (64px, replaces the default Next icon), `apple-icon.png` (180), and PWA `icons/icon-192.png` + `icon-512.png` (emblem centred on black, maskable-safe). Added `app/manifest.ts` (Web App Manifest — name/short_name, `display: standalone`, `start_url: /`, `theme_color #0E6B6B`, `background_color #FBFAF5`) so members can "Add to Home Screen". Layout gains `appleWebApp` metadata + `themeColor` viewport. Landing page now opens with the **emblem mark, top-centre** (used the emblem-only, black-keyed to transparent, rather than the full lockup — the hero already carries the "Jay Hatkesh / જય હાટકેશ" wordmark, so the lockup would double the name). — **Checks:** static public assets only, no input/secrets. Typecheck + lint clean (stale `.next/dev/types` cleared); landing (200) + `/manifest.webmanifest` (200, valid JSON) + `/favicon.ico` + `/apple-icon.png` + emblem/icon PNGs all 200; head emits `rel=icon`/`rel=manifest`/`apple-touch-icon`/`theme-color`; no dev-log errors.
- 2026-05-29 — UI — FAQ page (founder request). New public `/faq` page rendering the community-voiced `FAQ.md` content as collapsible `<details>` accordions across 15 sections (~55 Q&As), faithful to the source wording. Content lives in a structured module `faq/faq-content.ts` (`FAQ_INTRO`, `FAQ_CLOSING`, `FAQ_SECTIONS`) kept in sync with `FAQ.md`. Page carries a prominent **"Submit a query" navigation CTA → /contact** (the "navigation CTA to submit query" the founder asked for), plus the existing FAQ link in the landing footer points here. — **Checks:** static content, no user input, no DB, no secrets — public read-only page. Typecheck + lint clean; /faq (200) + /contact (200) smoke-tested, content rendered (heading + a known Q + CTA present), no runtime errors in dev log.
- 2026-05-29 — UI/Email — Contact form (founder request). New public `/contact` page: Name + Email + Phone(optional) + Message + a **server-checked simple math captcha** (numbers generated per-request server-side via a helper module — kept out of the component body to satisfy the render-purity lint rule; verified `answer === a+b` in the action). `submitContactQuery` validates + emails the query to the project inbox (`contact.email`) via Resend with **reply-to = the submitter** so the founder can reply directly. **Email-only** (no DB row — the inbox is the collection; avoids a public anon spam-table). `sendEmail` extended with optional `replyTo`. Landing footer now carries **FAQ · Contact · Terms · Privacy** links (the "Contact link in the footer" the FAQ references). — **Checks:**
  - Captcha is a light anti-bot guard (per founder's "simple captcha" ask), verified server-side; a determined bot could still solve it — note for later (stronger captcha/rate-limit if spam appears). The email send is best-effort; a failure returns a friendly error.
  - User input HTML-escaped in the email template; reply-to lets the founder respond without exposing the project inbox in threads.
  - Typecheck + lint clean; /contact (200, public) + landing (200) compile.
  - **NEXT (slice B)**: `/faq` page rendering FAQ.md (14 sections) with a "submit a query" CTA → /contact.
- 2026-05-29 — Email — Welcome email (warm founder-voice). New `welcomeEmail` template: opens + closes with **જય હાટકેશ**, a personal note from an **anonymous fellow Nagar in Ahmedabad** explaining the **self-funded, no-ads/no-investors/no-commission** ethos and **why each member's joining strengthens the whole circle** (network effect + seva + belonging), with a complete-your-profile CTA. Migration `20260529140000_welcomed_at.sql` (0032, applied): `members.welcomed_at` send-once flag (+ re-grant). Wired in `/auth/callback`: on first successful sign-in, if `welcomed_at` is null + email present → send welcome + stamp `welcomed_at` — **best-effort, never blocks the redirect**, fires exactly once. — **Checks:** self-email (own row via the post-exchange session); typecheck + lint clean; /auth/callback (no-code → /join?error=auth) + /join (200) compile. Copy shared with founder for wording/Gujarati refinement. Delivery pending Resend env.
- 2026-05-29 — UI — Name-disambiguation pass (founder identity-collision concern: two same-name, even same-DOB members can coexist; identity is phone/email, names can collide). Wherever a member name previously appeared **alone**, it now carries context: `/connections` (incoming / approved / outgoing — `memberLabel`) and `/listings/leads` (seeker — `seekerLabel`) now render **name · city + a ✓ ID-verified mark**. Pulled from `members_directory` (city_id + id_verification) + a cities lookup. — **Checks:** read-only display change; no schema/RLS touch; no DOB exposed (city + verified status are the disambiguators). The directory + feed already carried this context. Typecheck + lint clean; /connections + /listings/leads compile. Identity model unchanged (phone NOT NULL UNIQUE = the key).
- 2026-05-29 — Email — Cross-member notifications (slice 2b, test-phase). Migration `20260529120000_get_member_email_fn.sql` (0031, applied to remote): `get_member_email()` SECURITY DEFINER — returns a member's email **only when an inquiry or connection request already links the caller and target** (no harvesting); consumed server-side to send, never returned to a client; excludes members pending deletion. New templates `leadEmail` + `connectionRequestEmail` (recipient name omitted for privacy; note escaped). Wired: `expressInterest` → emails the **provider** "someone's interested in your listing" (the just-inserted inquiry satisfies the gate); `requestConnection` → emails the **recipient** "new connection request" (the just-inserted request satisfies the gate). Both **best-effort** (never block the action). — **Checks:**
  - **No email harvesting** — the gate requires a pre-existing lead/connection; a member can't pull arbitrary emails. Email used server-side only.
  - **Ordering**: the inquiry/request is inserted *before* the email lookup, so the relationship gate passes for the legitimate notify, and only then.
  - **TEMPORARY** — these are the test-phase email stand-ins for the WhatsApp Utility nudges; move to WhatsApp + in-app at WABA cutover (recorded in memory).
  - **Verified**: typecheck + lint clean; /feed + /connections + /directory compile. Delivery pending the founder's `RESEND_API_KEY` in Vercel.
  - **Still to do**: welcome email (deferred from 2a), cron-driven listing expiry-reminder/expired emails (need DB-side send via pg_net + Vault key), and the name-disambiguation pass.
- 2026-05-29 — Email — Transactional email infra + self-emails (slice 2a). New `lib/email/send.ts` (Resend API via fetch — `RESEND_API_KEY`/`EMAIL_FROM`; **best-effort: no-ops with a log if the key isn't set, never breaks the calling action**) + `lib/email/templates.ts` (branded inline-HTML layout; user values HTML-escaped). Wired the **self-emails** (the member emailing themselves about their own account — own email read via RLS SELECT-own, no cross-member exposure): account-deletion **scheduled** (with the erase date) + **cancelled** (deletion-actions), and **listing published** ("live, expires {date}") on the free-publish path (setListingStatus). — **Checks:**
  - App transactional mail goes via the **Resend API** (this helper); the sign-in magic-link goes via **Supabase Auth SMTP** (founder configuring) — two separate paths, both Resend.
  - Sends are **best-effort/non-blocking** — wrapped so a missing key or Resend error never fails the deletion/publish action.
  - No cross-member email access here (all sends are to the current user's own email). Phone-OTP users with no email simply don't get the email (skipped).
  - **Verified**: typecheck + lint clean; /profile + /listings compile. Actual delivery is the founder's to confirm once `RESEND_API_KEY`+`EMAIL_FROM` are in Vercel.
  - **NEXT (2b)**: cross-member notifications (lead-on-your-listing, connection-request) via a relationship-gated `get_member_email()` definer fn; cron-driven listing expiry-reminder/expired emails need DB-side send (pg_net + key in Supabase Vault) — separate. Then the name-disambiguation pass.
- 2026-05-29 — Auth — Real consented email onboarding (test-phase bridge to WhatsApp OTP). Migration `20260529100000_email_onboarding_real_phone.sql` (0030, applied to remote): rewrites `handle_new_auth_user()` so the **email** branch harvests the member's **real phone + consent** from `raw_user_meta_data` (field-whitelisted: opt_in_whatsapp/terms_*/phone only — never role/trust) instead of the 0018 placeholder; robust fallback to a minimal placeholder-phone row on any collision so auth never breaks. Renamed `/test-sign-in` → **`/join`** — now a proper consented onboarding: email + **real WhatsApp number** (country code + national) + **Terms acceptance + WhatsApp opt-in**, passed via `signInWithOtp({ options: { data: { ...consent, phone } } })`. `/auth/callback` error fallback + the landing "Join the community" CTA now point to `/join` (test phase; revert to `/sign-in` at WABA cutover). — **Checks:**
  - **Phone stays the universal unique key** even for email-onboarded testers → contact-reveal/connect/donor/notifications work. Metadata harvest is whitelisted (same security posture as the 0014 consent harvest); fallback prevents auth failure on phone/email UNIQUE collision.
  - **Consent captured**: Terms (TERMS_VERSION) + WhatsApp opt-in recorded on the email path (parity with the phone gate). Phone is captured but **not OTP-verified** in this bridge — acceptable for the invited test cohort; flagged.
  - **Cutover note** (recorded in memory): an email auth identity ≠ a future phone-OTP identity in Supabase — at WABA cutover, link identities or migrate these accounts. The captured phone keeps them reachable meanwhile. `/join` + the 0030 email branch are explicitly TEMPORARY.
  - **Verified**: typecheck + lint clean (after clearing a stale `.next` type cache that referenced the removed route); `/join` 200, `/test-sign-in` 404, landing 200.
  - **NEXT today**: email templates via Resend (transactional + temp lead/connection notifications) — needs `RESEND_API_KEY`+`EMAIL_FROM` in Vercel + Supabase Auth SMTP→Resend (founder); then the name-disambiguation pass.
- 2026-05-28 — Data/UI — Phase 1 §4 seeker side: Requests (the demand side). Migration `20260528400000_request_contact_fn.sql` (0029, applied to remote): `get_request_contact()` SECURITY DEFINER (open request → seeker phone, for "I can help"). UI: `/requests/new` (free demand post — category, city/area, budget, needed dates, PG gender/food prefs shown only for PG, details), `/requests` (your requests — close/reopen/delete), and the **Living Feed now has an Offers | Requests toggle** (`/feed?type=requests`) — request cards show "Looking for: X" + budget/city/dates + "I can help" → reveals the seeker's WhatsApp. requests/actions (`createRequest`, `setRequestStatus`, `deleteRequest`, `respondToRequest`). Hub Find card → "Post a request"; quick links add "Your requests". — **Checks:**
  - Requests are fully member-managed (RLS CRUD-own, public-read; no admin-only columns → no grant gaps, no gating fn needed). `createRequest` sets member_id=caller (RLS WITH CHECK); `setRequestStatus`/`deleteRequest` scoped `.eq(member_id)`.
  - "I can help" reveals the seeker's contact only for **open** requests, via the definer fn (seeker phone isn't cross-member readable) — connector model (the seeker posted publicly seeking help). Own requests excluded from the browse.
  - No request-side lead table (no inquiries schema for requests) — providers reach the seeker directly via WhatsApp; the public request IS the post. Recorded.
  - **Verified**: typecheck + lint clean; /requests, /requests/new, /feed?type=requests compile + auth-gate (307). The two-sided marketplace (offers ⇄ requests) is now live end-to-end for free posts.
- 2026-05-28 — Data — Phase 1 §4 listing expiry cron (closes the period/auto-closure loop). Migration `20260528380000_expire_listings_cron.sql` (0028, applied to remote): `expire_listings()` SECURITY DEFINER (flips `active` listings whose `expires_at` has passed → `expired`) on a daily pg_cron job (03:15 UTC; pg_cron already enabled in 0023). Pairs with `set_listing_status`'s publish-from-`expired`, so the full free loop is: publish → active for the term → auto-expire → re-publish (renew). EXECUTE revoked from members. — **Checks:** verify in SQL editor `select public.expire_listings();` (returns 0 — nothing past term yet) + `select * from cron.job where jobname='expire-listings'`. No app changes — `/listings` already renders 'expired' with a Publish (renew) action. **Pre-expiry reminders (~day 25/28) deferred** — they need a notification channel (in-app notifications table, or email via Resend, or WhatsApp via Meta) that isn't built yet; the auto-closure itself is done. Paid (Business/Education) renewal rides the payment flow when it lands; term is the hardcoded 60-day free default until the §4 admin pricing/settings config exists.
- 2026-05-28 — Data/UI — Phase 1 §5 Living Feed + §4 inquiry→lead→connect loop. Migration `20260528360000_listing_contact_fn.sql` (0027, applied to remote): `get_listing_contact()` SECURITY DEFINER — returns an active listing's contact (per-listing contact_whatsapp/phone, else owner's phone) as the source for the wa.me deep-link (owner phone isn't cross-member readable). New `/feed` (browse active listings, excludes own; filters by category + city; cards show title/category/price/area+city/owner + ID-verified badge), `feed/actions.ts#expressInterest` ("I'm interested" → inserts an `inquiries` lead → reveals "Continue on WhatsApp"), `connect-listing.tsx` (the interest + reveal UI), `feed-filters.tsx`, and `/listings/leads` (provider Lead Inbox — inquiries on the member's listings, with seeker names). Hub "Find" card → /feed; quick links add Feed + Your leads; /listings header gets a Leads link. — **Checks:**
  - **Connector loop, consent-appropriate**: a provider published the offer, so interest opens the channel directly (no approve step) — distinct from the directory's person-to-person request→approve. Contact is revealed only via the definer fn (active listings only); the raw owner phone is never selected client-side.
  - **Lead trail**: `inquiries` RLS already gives the owner their inbox + the seeker their own; no DELETE (lead history preserved). expressInterest sets seeker_id=caller (RLS WITH CHECK) against an active listing.
  - **WhatsApp business-initiated dual nudge deferred** (needs Meta WABA + approved Utility template) — for now the seeker gets the wa.me deep-link and the provider sees the lead in-app. Recorded.
  - **Own listings excluded from the feed**; members_directory used for owner names (so a pending-deletion owner's listings show a neutral fallback). For email-test providers the wa.me number is a placeholder (won't dial) — same known caveat.
  - **Verified**: typecheck + lint clean; /feed + /listings/leads compile + auth-gate (307). The full create→publish→feed→interest→lead→WhatsApp loop is now in place for free categories.
- 2026-05-28 — Data/UI — Phase 1 §4 Listings slice 1: Create-a-Listing + manage + free publish. Migration `20260528340000_listings_foundation.sql` (0026, applied to remote): adds `listing_categories.is_paid` + `verification` (locked routing — business=paid/admin, education=paid/open, room/vehicle/pg=free/id_verified, goods/tour/service/expert=free/open); **closes a real hole** — the slice-3 grants let a member set `status` and (default INSERT) insert an `active`/`fee_paid` row, i.e. self-publish a PAID listing without paying. INSERT/UPDATE grants now cover **content columns only** (no status/lifecycle); status changes go through the new `set_listing_status()` SECURITY DEFINER fn (publish[free, with id-verification gate; raises `payment_required` for paid] / pause / resume). UI: `/listings/new` (offer-side persuasion — Earning/Security/Safety/સેવા — + category picker + category-aware form → **draft**), `/listings` (own listings across statuses; publish/pause/resume/delete via the gated fn), server actions (`createListing` — time_binding taken from the category, never the client; `setListingStatus`; `deleteListing`). Hub "Offer" card now → Create a listing; quick links add "Your listings". Connector disclaimer on the form + the list. — **Checks:**
  - **Self-publish hole closed**: members can't INSERT or UPDATE their way to `status='active'`/`fee_paid`/lifecycle — only content. Activation is the definer fn, which enforces the per-category verification gate and blocks paid categories (they'll publish via the payment flow). New listings are always inert drafts.
  - **Free publish** sets `published_at` + `expires_at = now()+60d` (soft term; admin-config later) + `status='active'`. Paid (business/education) publish raises `payment_required` → friendly "coming soon" in the UI (slice 2 = Razorpay).
  - **Connector ethos**: price is free-text ("money is offline"), disclaimer present; exact address for room/pg/vehicle is shared member-to-member on connect, never published (form copy + the schema's area-vs-address split).
  - **Verified**: typecheck + lint clean; /listings + /listings/new compile + auth-gate (307). Authed create/publish is the founder's to exercise.
  - **NEXT (slice 2)**: Razorpay payment → publish for paid categories (admin pricing/settings table for price+term; server-verified signature → payments 'paid' → listing active+lifecycle). Then expiry/reminder cron (mirrors the deletion purge), renewal, and the §5 feed (where active listings actually get browsed — they're currently only visible to the owner + via directory-less paths until the feed lands). Edit-listing also deferred.
- 2026-05-28 — UI — Phase 1 §3 Community Intelligence ("the scoop") v1. New `/intelligence` page (auth-gated): headline totals (members/professionals/doctors/cities from `community_pulse`), a **by-profession breakdown** (`community_pulse_by_profession`, each row → `/directory?profession=<id>` to drill into the member list), and a **capability "Ready to help" layer** (distinct members per capability kind, aggregated in JS from `member_capability_directory`). Linked from the hub ("Community insights") + the directory header ("Insights"). — **Checks:** aggregate-only, counts visible to members; individual contact stays permission-gated (drill-through goes to the directory + the existing connection flow). Reads existing views only — no new tables/RLS. Reads sparse now (1 member) but it's the permanent flagship surface, correct + ready as members join. The richer composable multi-dimension faceting (compose city + profession + capability + age/gender/blood) can layer on later; the dimension views already support it. Typecheck + lint clean; /intelligence compiles + auth-gates (307).
- 2026-05-28 — UI — Phase 1 §3 Onboarding CTA hub (signed-in home). The landing page now branches: **guests** see the locked marketing landing (unchanged); **signed-in members** get the intent hub (`signed-in-home.tsx`) per SPEC §7.05 + hard constraint #9 (intent-based UI, always). Three intent cards — **Connect** (→ /directory), **Find** (→ /directory; rooms/rides/jobs marked "coming soon"), **Offer** (→ /profile; listings "coming soon") — plus quick links (connections, edit profile), a profile-completion nudge when required fields are missing, and a quiet Community-Pulse line ("N members across M cities…"). Banner now greets by name (not the ugly `test:` phone) and shows an **Admin** link for admins. Replaces the placeholder "signed-in sees the marketing landing" from the original landing slice. — **Checks:** no new tables/RLS; reads the member's own row + the anon-safe `community_pulse`. Live links only to built surfaces; unbuilt §4/Phase-2 intents shown as honest "coming soon" rather than dead links. Typecheck + lint clean; guest landing still renders the marketing content (200); hub path compiles (authed view is the founder's to exercise). Most CTA targets (listings/matrimony/jobs) await §4 — the hub is the IA spine they'll slot into.
- 2026-05-28 — Data/UI — ID verification slice 2: admin review surface (the FIRST admin area). Migration `20260528320000_admin_review_verification.sql` (0025, applied to remote): `admin_review_verification(verification_id, approve, notes)` — SECURITY DEFINER + `is_admin()`-gated; stamps the verifications row (status/reviewed_by/reviewed_at/notes) and on approve sets `members.id_verification='verified'` + `trust_level = greatest(trust_level, 2)` (on reject → back to 'none' so the member can re-submit). New role-gated `/admin` area: `admin/layout.tsx` (reads own `members.role`; redirects non-admins — `/sign-in` if logged out, `/` if not admin), `admin/verifications/page.tsx` (lists pending document submissions, resolves member names, mints a 300s **signed URL** per ID doc, "View ID ↗"), `admin/verifications/review-buttons.tsx` + `admin/actions.ts` (Approve/Reject + optional note → the RPC). `/profile` shows an "Admin →" link when `role='admin'`. Founder's test account was set `role='admin'` via SQL. — **Security checks:**
  - **Defense in depth on admin power**: the UI gate (layout role check) is convenience; the REAL enforcement is the `is_admin()` check inside the SECURITY DEFINER function — a non-admin calling the RPC directly gets "not authorized". Members still can't set their own `id_verification`/`trust_level` (not in the grant); only this function does, running as owner.
  - **ID docs stay private**: admins view via short-lived (5 min) signed URLs minted server-side; the bucket is private and only `is_admin()` (storage RLS from 0024) can read others' folders. No public exposure.
  - **Approve is idempotent-safe** (`greatest()` never downgrades trust); reject only resets a still-'pending' member.
  - **Verified**: typecheck + lint clean; `/admin/verifications` + `/profile` compile + auth-gate (307). This completes the ID verification flow end-to-end (submit → admin review → verified badge + L2 trust). The verified badge already renders in the directory + profile from earlier slices.
  - **Admin area is now the reusable foundation** for §7 moderation, §7d help-drive verification, §6 magazine editing.
- 2026-05-28 — Data/UI — ID verification slice 1: member submission (Phase 1 §2). Migration `20260528300000_id_verification_storage.sql` (0024, applied to remote): a **private** `id-documents` Storage bucket (5 MB cap, image/PDF only) + storage.objects RLS (a member writes/reads only their own `{member_id}/…` folder; **admins read all** for the slice-2 review surface); a trigger that flips `members.id_verification` 'none'→'pending' when a document verification row is inserted (the column isn't member-grantable, so the trigger sets it). New `/profile` card (`id-verification.tsx`): when status is none → upload a government ID (client + bucket both enforce type/size) → file goes to `{member_id}/id-document` (upsert) + a `verifications` row (method='document', the existing 0005 member-INSERT policy) → status shows "under review"; verified → green "✓ verified". — **Security / privacy checks:**
  - **ID docs are private + scoped.** Bucket is non-public; storage RLS limits a member to their own folder; admins (`is_admin()`) read all for review. Other members can never see anyone's ID. Reassured in the UI copy.
  - **Type + size enforced twice** — client (accept + JS checks) AND the bucket (`allowed_mime_types` + `file_size_limit`) — so a tampered client can't bypass.
  - **Member can't self-verify**: `members.id_verification` + `trust_level` stay out of the member grant; submission only reaches 'pending' via the SECURITY DEFINER trigger; setting 'verified' + trust_level is admin-only (slice 2). The member INSERT on verifications is still pinned to status='pending', no reviewer fields (0005 policy + WITH CHECK).
  - **Verified**: typecheck + lint clean; /profile compiles + auth-gates. Authenticated upload is the founder's to exercise.
  - **Slice 2 (NEXT) — admin review surface**: a role-gated `/admin/verifications` listing pending docs, viewing each via a signed URL, and approve/reject through a SECURITY DEFINER `admin_review_verification()` (is_admin-gated) that sets `id_verification='verified'` + `trust_level` and stamps the verifications row. Needs the founder's member row set to `role='admin'` (a one-line SQL update — id_verification/role aren't member-editable). Until then, submitted IDs sit 'pending'.
- 2026-05-28 — Data — Purge cron for account deletion (closes the gap from the deletion slice). Migration `20260528280000_purge_deleted_accounts_cron.sql` (0023, applied to remote): enables `pg_cron`, adds `public.purge_deleted_accounts()` (SECURITY DEFINER — deletes `auth.users` for members whose `deletion_requested_at` is older than 30 days; cascades to members + all owned rows = full erasure), and schedules it daily at 03:30 UTC. **Done entirely in Supabase — no Vercel Cron, no CRON_SECRET, no service-role key in the app** (founder noted Vercel cron isn't set up; Supabase is). EXECUTE revoked from anon/authenticated/public (only the cron + an admin run it). — **Checks:**
  - **One-statement DPDP erasure**: deleting the `auth.users` row cascades via `members.id ON DELETE CASCADE` through professions/capabilities/connection_requests/etc.
  - **Couldn't verify the runtime delete permission from here** (MCP `execute_sql` blocked; can't run the job). The migration applied (extension + function + schedule created). **Founder to verify** in the Supabase SQL editor: `select public.purge_deleted_accounts();` (returns 0 now — nothing has aged out — and confirms no permission error) and `select * from cron.job where jobname='purge-deleted-accounts';`. If the delete-from-auth.users permission is denied for the function owner, fall back to a service-role Edge Function called via pg_net.
  - With this, the self-serve deletion flow is end-to-end: request → deactivate → 30-day cancel window → automatic erase.
- 2026-05-28 — Data/UI — Self-serve account deletion w/ grace period (Phase 1 §2/§8 — DPDP erasure; the in-app route the `/data-deletion` page promised). Migration `20260528260000_account_deletion.sql` (0022, applied to remote): `members.deletion_requested_at` (+ added to member UPDATE grant + a partial index for the future purge scan); `members_directory` re-stated to EXCLUDE members pending deletion (propagates to the chained profession/capability directories); `get_revealed_contact()` updated to not reveal a pending-deletion member. New: `deletion-actions.ts` (requestAccountDeletion / cancelAccountDeletion — own-row, RLS-scoped), `account-deletion.tsx` (a profile danger-zone: "Delete my account" with a two-tap confirm → sets deletion_requested_at=now; when pending, a red banner showing the scheduled erase date + "Cancel deletion"). Wired as a card at the bottom of `/profile`. — **Checks & posture:**
  - **Immediate deactivation on request**: a pending-deletion member vanishes from the directory + Community-Intelligence views and becomes unreachable (contact reveal returns nothing) the moment they request — enforced in SQL (view WHERE + function guard), not just UI.
  - **Self-controlled, reversible**: request + cancel both run as the authenticated user (RLS own-row); the action sets the timestamp server-side (member never supplies it). 30-day grace, cancellable by signing in.
  - **No new env needed for this slice** — request/cancel use the existing anon/authenticated path.
  - **PURGE CRON IS THE REMAINING PIECE (follow-up, needs founder env setup):** the actual erase of accounts past the grace window needs a service-role cron — a `CRON_SECRET`-protected route that deletes `auth.users` where `deletion_requested_at < now()-30d` (cascade removes the members row), scheduled via Vercel Cron. Requires `SUPABASE_SERVICE_ROLE_KEY` + `CRON_SECRET` in Vercel + a `vercel.json` cron. Until then, deletion = deactivation + scheduled-state; data isn't yet physically erased, so DPDP erasure is not fully closed. Flagged.
  - **Verified**: typecheck + lint clean; /profile + /directory compile + auth-gate. Authenticated flow is the founder's to exercise.
- 2026-05-28 — Data/UI — Blood group + community donor matching (Phase 1 §3, founder direction). Migration `20260528240000_blood_group_donor.sql` (0021, applied to remote): `members.blood_group` (CHECK = 8 ABO/Rh groups, nullable) + `members.willing_to_donate` (bool, nullable), both added to the member UPDATE grant; `members_directory` re-stated with a derived `donor_blood_group`. Profile (optional section) gains a blood-group dropdown with a **red double-check warning** + a "Ready to donate when a fellow Nagar needs it?" Yes/No. Directory gains a **Blood donor** filter (by group) + a red "Blood donor · O+" badge on cards. Landing hero gains a bullet: "Reach a willing blood donor in the community when it matters most." — **Security / privacy (DPDP — blood group is sensitive health-adjacent data):**
  - **Opt-in + purpose-limited + privacy-gated exposure.** `donor_blood_group` in the directory view is populated ONLY when the member `willing_to_donate = true` AND is in the **18–65** donation window (`age(date_of_birth)`) AND has a group set — otherwise NULL. So a member's blood group reaches other members ONLY if they explicitly volunteered as a donor and are age-eligible. Non-donors' blood group stays on their own row (members RLS = SELECT-own), shown only on their own profile, never in the view. This is the "smart system identifies who can/may/might donate" — encoded in SQL, not just UI.
  - **Controlled list** (8 groups, DB CHECK + app validation) — no free text, so it aggregates cleanly for any future donor-intelligence view.
  - **Contact still consent-gated** — finding a donor surfaces the badge; reaching them goes through the existing connection request → approve (or openly-contactable) flow. The app never brokers the donation; connector ethos intact.
  - **Age window 18–65** is the India NBTC range, encoded in the view (single place to change).
  - **Verified**: typecheck + lint clean; landing (200), /profile + /directory + /directory?blood=O+ compile + auth-gate. Authenticated view is the founder's to exercise (OTP/email).
- 2026-05-28 — UI/Data — Profile v2 (founder direction, live-test feedback): immutable name/DOB + warm directory blurb + green surname badge. Migration `20260528200000_immutable_name_dob.sql` (0019, applied to remote): a BEFORE-UPDATE trigger on `members` that silently keeps the old `full_name`/`date_of_birth` if a change is attempted once they're set — the hard guarantee behind the founder rule "name + DOB never change; everything else editable". `updateProfile` now reads the current row, skips validating/writing those two fields when already set, and the profile form renders them read-only (with a "can't be changed" note) after first save. Recognised-surname badge recoloured **green** (`brand-success`) on both profile + directory (founder expected green; was heritage-gold). Directory cards now lead with a **warm auto-blurb** ("{Name} is a/an {profession} from {city}. {their bio}") composed from fields + the member's bio — pronoun-free so it's correct for any gender; the redundant city line + profession chips were folded into it (capability "Open to:" chips kept). `members_directory` already exposes `bio`; added it to the directory select. — **Checks:**
  - **Immutability enforced in depth**: UI read-only + action skip + DB trigger. The trigger means even a tampered direct API write (members has the column grant on these) can't change them. Silent-keep (not raise) so a normal save re-sending the same value never errors.
  - **No new RLS surface**; blurb is composed from already-exposed safe view columns (no phone/email/DOB). Gender-aware-by-omission (no pronouns) per the founder's "gender in mind".
  - **Verified**: typecheck + lint clean; `/profile` + `/directory` compile + auth-gate (307 → /sign-in). Authenticated view not browser-tested by me (OTP/email path is the founder's to exercise).
  - **Still open from the same feedback** (NOT in this slice): account-deletion flow (self-serve + grace period — chosen; needs new columns + deactivation propagation + instructions + a purge cron); richer "Offer something" capability fields (awaiting founder's field list); recognised-surname badge not appearing for "Buch" — root cause pending (seed is English-only; confirm the exact surname text/script).
- 2026-05-28 — Data/UI — Phase 1 §2 "Recognised Nagar surname" soft badge. Migration `20260528160000_recognised_surnames.sql` (0017, applied to remote): a `nagar_surnames` reference table (normalised lower/trim surnames, conservative founder-curatable starter seed of ~27 commonly-cited Nagar surnames, deliberately excluding broadly-shared Gujarati names like Shah/Parikh to avoid false positives), a SECURITY DEFINER BEFORE-trigger `set_recognised_surname()` that derives `members.recognised_surname` whenever surname is set/changed, RLS (authenticated-read, admin-write), and a backfill. Profile header now shows a "✓ Recognised Nagar surname" pill when true; the directory already renders the badge. — **Security & connector checks:**
  - **Signal only, never a gate** (CLAUDE.md #5 / AGENDA §2) — recognised_surname affects no access, trust level, or eligibility; purely a soft affirmation.
  - **Members can't self-claim it** — recognised_surname stays out of the member UPDATE column grant (0005/0014/0016); the trigger sets it on NEW (column grants restrict the statement's named columns, not trigger-assigned values), so the badge is always derived, never asserted. Trigger is SECURITY DEFINER + `search_path=public` and fires only on insert / `update of surname`.
  - **nagar_surnames RLS**: authenticated-read (a future admin/profile UI may list it), no anon read, no member writes — curation is admin/service-role only.
  - **Founder curation flagged loudly** in the migration header — the seed is a starter; Nagar surnames are culturally specific and the founder is the authority. Over/under-inclusion only moves a soft badge. Gujarati-script spellings can be added as rows.
  - **Verified**: typecheck + lint clean; `/profile` compiles + auth gate works. Authenticated view not browser-tested (OTP blocker). Migration applied cleanly (deterministic DDL; MCP column-privilege check still unavailable here).
  - **Remaining §2 item — ID verification flow — NOT started.** It's a larger slice: a private Storage bucket for ID photos (Storage was deferred for profile photo too), an upload UI that writes a `verifications` row (table + member-INSERT-pending RLS already exist from 0005), AND a brand-new admin review surface to approve/reject → set members.id_verification + trust_level. Recommend doing it as its own focused effort (Storage + first admin surface).
- 2026-05-28 — Data/UI — Phase 1 §3 Directory + permissioned contact reveal (find each other + reach with consent). Founder decisions: **hybrid reach model** (default request→approve; a member can flip an "openly contactable" switch to be reached directly; capability opt-ins count as consent for their field) revealing a **WhatsApp deep-link**. Migration `20260528140000_directory_connections.sql` (0016, applied to remote): adds `members.openly_contactable bool default false` (+ column grant), `connection_requests` table (requester/recipient/note/context/status pending|approved|declined + RLS), the `get_revealed_contact(target)` SECURITY DEFINER function, and adds `openly_contactable` to the `members_directory` view. New routes: `/directory` (filters by profession/specialty/city/sub-community over the 0013 definer views; member cards with profession + capability chips + verified/Nagar-surname badges; per-member Connect state) and `/connections` (incoming approve/decline, outgoing pending, approved connections with WhatsApp). Files: `directory/page.tsx`, `directory/directory-filters.tsx`, `directory/connect-button.tsx`, `connections/page.tsx`, `connections/respond-buttons.tsx`, `connections/actions.ts`. Profile gains an `openly_contactable` toggle; landing banner + profile link to `/directory`. — **Security & connector checks:**
  - **Phone is never in directory data.** The directory reads only the 0013 definer views (phone/email/DOB excluded by column whitelist). A member's phone leaves the DB ONLY through `get_revealed_contact()` — SECURITY DEFINER, returns a row only when consent holds: target is `openly_contactable` OR an **approved** `connection_request` links caller↔target (either direction = mutual consent), and never for self. `revealContact` returns a `wa.me` deep-link (number embedded in the href, never returned/displayed as copyable text — matches the founder's "deep-link only" choice).
  - **connection_requests RLS**: SELECT = either party (+admin); INSERT = requester is caller, status pending, not self (WITH CHECK); UPDATE = recipient only. Column grants: INSERT limited to requester_id/recipient_id/note/context; UPDATE limited to status/responded_at — a recipient cannot rewrite the note or re-point the request, and neither party can forge `responded_at`/status combinations the CHECK forbids. `auth.uid()` wrapped as `(select auth.uid())` per the perf rule. Unique (requester,recipient) blocks request spam.
  - **respondToRequest** scopes update to `.eq(recipient_id, me).eq(status,'pending')` (belt-and-braces atop RLS) — only the recipient resolves a still-pending request.
  - **openly_contactable defaults false** (privacy-safe default); member-settable via the extended column grant; role/trust/id_verification/phone still admin-only.
  - **Directory excludes self + incomplete profiles** (`full_name is null` filtered out); auth-gated (307 → /sign-in). Profession/specialty filter narrows via `member_profession_directory`; city/sub-community filter directly on `members_directory`.
  - **Couldn't verify column privileges / function ACL via MCP** (Supabase MCP `execute_sql` permission-denied here), but the DDL applied without error and is deterministic. **Authenticated end-to-end NOT browser-tested** — same OTP blocker (Meta WABA pending); routes verified to compile + gate only. Typecheck + lint clean.
  - **In-app notification only for now** — a new connection request shows in the recipient's `/connections` inbox; WhatsApp/push notification of the request is deferred (needs Meta approval + an approved Utility template). Recorded for the §4 notification work.
- 2026-05-28 — UI/Data — Phase 1 §2 capability opt-ins (`member_capabilities` — the સેવા offers). The `/profile` page gains a fourth card: add/edit/remove capabilities of kind expert_guidance | mentor | other, each with a free-text domain + optional description + an "actively offering" toggle (`is_offered`, pause without deleting). Carries the **locked SPEC §7.04 encouragement copy** ("…you could be the answer to a fellow Nagar's need") as the motivating header BEFORE the opt-ins. Files: `capabilities-editor.tsx`, `capability-actions.ts`, `constants.ts` additions, `page.tsx` wiring. — **Security & connector checks:**
  - **Writes run as the authenticated user** → "Members manage own member_capabilities" policy (auth.uid() = member_id) is the boundary; no admin-only columns on the table, so no column-grant guard needed. Explicit `.eq(member_id, user.id)` on update/delete.
  - **All inputs validated server-side**: kind ∈ enum, domain required ≤80, description ≤280. Unique (member_id, kind, domain) collision handled with a friendly field error.
  - **Honesty rule preserved** — encouragement copy promises the possibility of helping, never a guaranteed outcome (connector ethos / DISPUTE.md). Consent-first: nothing is offered unless the member adds it; pausing (is_offered=false) keeps the row without exposing it as an active offer.
  - **Verified**: typecheck + lint clean; `/profile` compiles + auth gate works. Authenticated form flow not browser-tested (OTP blocker).
- 2026-05-28 — UI/Data — Phase 1 §2 member_professions UI ("What can you offer the circle?"). The `/profile` page gained a second card: a profession editor where a signed-in member adds / edits / removes their professions. Cascading profession → specialty (specialty select auto-appears only for professions that have specialties — Doctor/Engineer/Software/Lawyer/CA/Teacher per the seed), plus status (current/retired/studying), optional years_experience (0–99), optional expertise_text (≤280, the "what I actually do" freeform). Carries the §4 "What can you offer the circle?" nudge as its header. Files: `professions-editor.tsx` (client — shared cascading `ProfessionFields`, an add form, and per-row inline edit/remove; `useTransition` + result-in-callback, NOT `useEffect`+setState, to satisfy `react-hooks/set-state-in-effect`), `profession-actions.ts` (add/update/delete server actions), `constants.ts` (status options + limits), `page.tsx` (3 extra parallel queries: professions, specialties, member's own member_professions). — **Security & connector checks:**
  - **SECURITY FIX (gap found in migration 0005, closed here):** `is_verified` on `member_professions` was column-protected on UPDATE but **not on INSERT** — a member could self-set `is_verified = true` at insert time and spoof the admin credential badge (which this UI now displays). Migration `20260528120000_member_professions_insert_grant.sql` does `revoke insert … from authenticated; grant insert (member_id, profession_id, specialty_id, years_experience, expertise_text, status)` — `is_verified` (and id/created_at/updated_at) drop off the INSERT grant, matching the posture already used for `verifications` in 0005. Applied to remote via `supabase db push` (15/15 migrations synced; grant-only DDL, no data change). Couldn't run the column-privilege confirmation query — the Supabase MCP `execute_sql` is permission-denied in this environment — but the revoke+grant DDL is deterministic and applied without error.
  - **Writes run as the authenticated user** → "Members manage own member_professions" policy (`auth.uid() = member_id`) + the UPDATE/INSERT column grants are the enforcement boundary. Actions never send `is_verified`; explicit `.eq("member_id", user.id)` on update/delete is belt-and-braces on top of RLS.
  - **All inputs validated server-side**: profession_id/specialty_id UUID-shaped, status ∈ enum, years integer 0–99, expertise ≤280. **Specialty-belongs-to-profession cross-check** — the action queries `specialties.profession_id` and rejects a mismatch (e.g. "Cardiology" under "Lawyer"), protecting the Community Intelligence drill-down counts that depend on clean profession→specialty pairings. The cascading UI prevents bad pairings; the server check defends against tampered requests.
  - **Unique (member_id, profession_id) collision handled** — 23505 on add/update surfaces a friendly "already added / remove the duplicate" field error rather than a raw DB error.
  - **is_verified rendered read-only** — a "Verified" badge shows when admin-set; members have no control to flip it (now true on both INSERT and UPDATE).
  - **Honesty rule preserved** — nudge copy promises "finds the right person" (connection), never a guaranteed outcome.
  - **Verified at runtime** — typecheck + lint clean; fresh dev server compiles `/profile` and the auth gate redirects (307 → /sign-in) unauthenticated. The authenticated add/edit/remove flow was NOT browser-tested — same OTP blocker as prior §2 slices (Meta WABA pending; remote has no test-OTP path). Compile + gate verified, not a live form submission.
  - **Scope** — capability opt-ins (`member_capabilities`: expert_guidance/mentor/other) + the full સેવા encouragement screen are the NEXT slices (§2 smart progressive profiling / encouragement), deliberately not bundled here. This slice is the profession/expertise foundation those build on.
- 2026-05-28 — UI/Auth — Phase 1 §2 "Profile create/edit" shipped. New `/profile` route (web) lets a signed-in member fill + edit their identity fields. Files: `apps/web/src/app/profile/page.tsx` (server: auth-gate → fetch own member row + approved cities + sub_communities → render), `profile-form.tsx` (client: `useActionState`, uncontrolled fields with `defaultValue`, per-field + banner errors, cities grouped by country via `<optgroup>`), `actions.ts` (the `updateProfile` server action), `constants.ts` (gender enum + validation limits shared by form + action). Landing page signed-in banner now links to `/profile` (previously the route was unreachable). Fields per MEMORY "Profile fields": required = full_name, surname, city_id, pincode, gender, date_of_birth; optional = email, sub_community_id, bio. **No home address** — city + PIN only, with an inline privacy note ("we only ask for city and PIN — never your home address"). Email field carries the honest channel-strategy note ("receipts + OTP fallback only, never marketing"). Page shows a gold "finish required fields" banner until the profile is complete (`is_profile_complete` equivalent computed client-from-fetched-row). — **Security & connector checks:**
  - **Write path runs as the authenticated user, so RLS + column grants ARE the enforcement boundary.** The server action uses the cookie-bound anon client (NOT service_role), so migration 0005's "Members can update own row" policy + the column grant (only email/full_name/surname/city_id/pincode/sub_community_id/gender/date_of_birth/photo_url/bio/opt_in_* are grantable) apply. `role`, `trust_level`, `id_verification`, `recognised_surname`, `phone` are physically un-writable from this path even if the action tried — defence in depth on top of the explicit `.eq("id", user.id)` scope. The action only ever sends the 9 in-scope columns.
  - **All inputs validated server-side** (AUDIT checklist "All inputs validated/sanitised server-side"): required-present, length caps (name ≤80, bio ≤600), gender ∈ enum, DOB parseable + not-future + year ≥1900, email regex + ≤254 + lowercased, city_id/sub_community_id are UUID-shaped, pincode 3–12 alnum/space/hyphen. Client `maxLength`/`required`/`type=date min/max` are UX only — the action re-checks everything and never trusts the client. `noValidate` on the form deliberately routes validation through the server.
  - **PIN validated loosely on purpose** — global-from-day-one (MEMORY geography) means diaspora postal codes (US ZIP, UK alphanumeric) must pass, so no 6-digit Indian-PIN lock. Field labelled "PIN / postal code".
  - **Unique-email collision handled** — `members.email` is UNIQUE; a 23505 from the UPDATE is caught and surfaced as a field error ("already linked to another account") instead of a raw DB error.
  - **No new tables, no migration** — pure app layer over the existing `members` table + lookups. `cities` filtered to `status='approved'` (pending/merged/rejected suggestions never offered in the picker). Sub-community options render their Gujarati names (`lang="gu"`); body font already includes a Gujarati fallback, so glyphs render (hard constraint #8 satisfied for content).
  - **Auth gate verified at runtime** — dev server: `GET /profile` unauthenticated → 307 → `/sign-in`; `/sign-in` + `/` still 200; route compiles with no errors. Typecheck + lint clean.
  - **DEFERRALS (documented, not silent):**
    - **Profile photo upload deferred to its own slice** (new AGENDA §2 bullet). It pulls in a scoped Storage bucket + storage RLS + type/size validation + an upload widget — its own security surface per the AUDIT file-upload checklist item. `photo_url` column + its column grant already exist (0004/0005), so the follow-up is purely the Storage plumbing + widget.
    - **Bilingual field labels deferred to the §3 "Gujarati typography pass"** — this slice keeps English field labels to match the existing sign-in/landing convention rather than ship a piecemeal half-Gujarati surface. The cross-app Gujarati pass localizes labels holistically. Gujarati content (sub-community names) already renders.
    - **opt-in toggles NOT in this slice** — communication preferences (`opt_in_whatsapp` set at signup, `opt_in_email`) belong with progressive profiling + the DPDP `/data` consent centre (§8), not the identity form.
    - **AGENDA item ticked** (text/required/optional create-edit flow is built + working); photo upload tracked as a separate unchecked bullet so the next session has a correctly-scoped task instead of re-opening the whole item.
  - **Pre-existing note (not from this slice):** Next 16.2.6 warns `middleware.ts` is deprecated in favour of `proxy.ts`. Affects the existing session-refresh middleware; flagged for a future framework-housekeeping pass.
- 2026-05-27 — UI — Phase 1 §3 landing page rebuilt to the locked spec (SPEC §7.05 + MEMORY "Landing page must (first second)"). `apps/web/src/app/page.tsx` now does 3 of the 4 landing jobs: (1) belonging — explicit "The digital home of the Nagar samaj — worldwide." line under the title; (2) mission/soul — the LOCKED Option A copy ("Once, our community was a phone call away…") rendered as a quiet pulled blockquote with a heritage-gold left rule; (2b) the four benefit lines (find a Nagar doctor/lawyer/mentor · homes/rooms/rides · buy-sell within community · verified help drives) as a clean centered list; (4) Gujarati typography ≥ English applied at the landing surface — `Jay Hatkesh` + `જય હાટકેશ` rendered co-equal at `text-5xl sm:text-6xl`, motto's Gujarati `text-xl sm:text-2xl` above the English `text-sm` translation, salutation `સેવા · સહકાર · શિસ્ત` larger than its English line. CTA copy changed from neutral "Sign in" to the warmer "Join the community" matching the locked landing framing. Signed-in members get a minimal top banner with `phone` + sign-out, then the same landing as informational context (in-app home is a Phase 1 §3 follow-up). Tagline is now `text-xs tracking-[0.4em]` (was 0.3em) for a quieter pre-headline. — **Decisions & deferrals:**
  - **Job #4 "peep-not-transact" DEFERRED.** SPEC §7.05 calls for guests to see Community Pulse counters + a teaser feed + the latest Setusarjan cover. The `community_pulse` view exists from §1, but: 0 members, 0 listings, 0 published magazine issues today. Rendering zeros + empty placeholders is the literal opposite of "peep at a *living* community" and would actively undermine the trust the rest of the page is trying to build. Ships as a separate slice once there's content to peep at — pairs naturally with seeding the first members + the first magazine issue.
  - **AGENDA items NOT ticked.** "Landing page" covers all four jobs in one bullet — 3 of 4 done is not done. "Gujarati typography pass" is the cross-app pass (every other surface + mobile glyph QA); this slice applies the rule to ONE surface. Both stay [ ] until the deferred work lands. Audit log carries the partial-credit narrative.
  - **Copy is the canonical store.** `BELONGING_LINE`, `MISSION_EN`, `BENEFITS[]` are top-of-file consts in `page.tsx` — single source of truth for the page's content. When the Gujarati rendering of the mission lands (MEMORY notes "Consider a co-equal Gujarati rendering"), it goes here.
  - **Three-column gap rhythm**, no 2x2 grid for benefits — single column lets each line breathe at `sm:text-lg` and reads top-to-bottom on mobile without re-wrapping awkwardly.
  - **No new data dependencies introduced.** Page is still a Server Component that reads only `supabase.auth.getUser()` for the signed-in banner gate — same shape as before. No Pulse query, no magazine query, no listings query — those land with the peep-not-transact slice.
  - **Honesty rule preserved.** Benefit lines + mission copy promise connection/belonging, never guaranteed outcomes ("Find a Nagar doctor — anywhere in the world" — *find*, not *get treated free*). Matches MEMORY's honesty rule that applies to every member-facing surface.
  - **Lint clean + typecheck clean.** Will deploy via push → Vercel auto-build (framework preset now correctly Next.js per the prior fix).
- 2026-05-27 — Auth — Phase 1 §2 "Sign-up gate" ticked off. The web + mobile sign-in flows already collect `mobile + OTP + consent (Terms + WhatsApp opt-in)` with zero profile fields at the gate (per the AGENDA constraint); migration 0014 persists the consent payload through `signInWithOtp({ options: { data: buildConsentPayload(...) } })` → `raw_user_meta_data` → `handle_new_auth_user()` → `members.{terms_accepted_at, terms_version, opt_in_whatsapp}`. This audit pass closes the remaining gaps: (a) verified mobile parity with web — `apps/mobile/src/components/sign-in-screen.tsx` uses the same `buildConsentPayload`, the same six-country dial-code set, the same Terms + WhatsApp opt-in switches, and deep-links to the same `https://jayhatkesh.in/terms` + `/privacy` URLs that web uses; (b) shipped `/terms` and `/privacy` placeholder pages so the sign-in consent links resolve instead of 404'ing — both pages are draft posture (clearly banner-marked as such) but communicate the actual product stance (connector-only, no commission, listing-fee-only, WhatsApp opt-in mechanics, RLS-gated contact reveal, no home address, the named processor list Meta+Razorpay reviewers will look for). Both pages display `TERMS_VERSION` from `@nagarsetu/shared/consent` so the version a member accepts at the gate matches the version on the page. — **Security & connector checks:**
  - **No new attack surface introduced.** Both new pages are pure Server Components rendering static markdown-equivalent content. No data fetching, no user input, no mutations, no env-var dependencies. Bot-safe.
  - **Connector posture is the spine of the Terms.** Section 2 ("Connector — not a party") is the load-bearing clause: Jay Hatkesh is never a party to any member-to-member arrangement, never holds funds beyond the listing fee, never takes commission. Mirrors the CLAUDE.md hard constraint #1–4 word-for-word in spirit.
  - **WhatsApp opt-in section matches Meta's mandatory disclosure shape** — Privacy §4 names the two send categories (Utility nudges + Marketing fortnightly digest), explains how to opt out, and clarifies that the sign-in OTP itself (Authentication template) is not subject to opt-in. Meta WhatsApp app-review will look for exactly this section.
  - **Processor list named** (Supabase, Meta, Razorpay, Resend, Vercel, Anthropic) — Razorpay activation reviewers look for this in Privacy. Each named only for its operational function, no "we share data with partners for marketing" generic clause that would be misleading.
  - **Draft banner is unmistakable.** Both pages open with an amber-bordered note saying "Draft — pending legal review before public launch." Promises that *posture* won't change between draft and final, only wording. Honest framing — a casual reader knows they're looking at a working version.
  - **Mobile-web link parity.** The mobile sign-in deep-links via `Linking.openURL('https://jayhatkesh.in/terms')` — same URL web uses. One page serves both clients. Bumping `TERMS_VERSION` would require a re-acceptance flow on both.
  - **Lint clean** (4 React unescaped-entity warnings fixed pre-commit), **typecheck clean** on the web app.
  - **AGENDA item ticked.** "Sign-up gate" is the second §2 item; closes alongside the first §2 item ("Meta WhatsApp OTP via Supabase Auth Hook") which remains scaffolded but unticked pending Meta WABA approval per WHATSAPP.md.
- 2026-05-27 — Ops — **Vercel 404 misdiagnosis closed**: `live: false` + edge `NOT_FOUND` on every path was caused by Vercel project **Framework Preset = "Other"** (visible in Settings → Build & Deployment, surfaced via `framework: null` in the API). Build succeeded and emitted the Next.js route table normally, but Vercel didn't run the Next.js output adapter on it, so `/vercel/output` never got the Build Output API structure the edge needs to register routes. Set Framework Preset = Next.js + redeployed (build cache disabled to force fresh build); custom domain `https://jayhatkesh.in/` now serves the landing page from Mumbai edge. Also disabled Vercel Authentication (was on "Standard Protection" which had been gating production deployments behind SSO — wrong posture for a public landing page; the security boundary is Supabase RLS + the Auth Hook HMAC + the server-only env vars, not Vercel's team-membership wall). Saved as a feedback memory so the framework-preset trap doesn't burn 30 min on the next Vercel project.
- 2026-05-27 — Auth — Phase 1 §2 slice 1 (Meta WhatsApp OTP — Supabase Auth Hook endpoint scaffolded; blocked on Meta WABA + Authentication template approval before live). New: `apps/web/src/app/api/auth-hooks/send-otp/route.ts` (the Supabase Send-SMS hook target — verifies the Standard Webhooks v1 signature, calls Meta WhatsApp Cloud API with the canned Authentication-template payload, returns the `{ error: { http_code, message } }` shape Supabase expects on failure so the client SDK surfaces the correct OTP error). Two new helpers: `lib/auth-hooks/verify-supabase-signature.ts` (pure HMAC-SHA256 + timingSafeEqual + 5-minute timestamp skew window) and `lib/whatsapp/send-auth-template.ts` (Graph API POST with the OTP as the body parameter + the copy-code button parameter; discriminated union return so caller can distinguish network failure / 4xx / 5xx). Also: new `WHATSAPP.md` operational doc (Meta Business Manager → WABA → System User → Authentication template → Supabase dashboard wiring, all step-by-step) added to the CLAUDE.md document map. `.env.example` updated with the OTP hook block (replaces the stale MSG91 placeholder); `apps/web/.env.local` updated locally with empty placeholders. — **Security & connector checks:**
  - **Public endpoint is HMAC-gated.** Without signature verification, anyone discovering the URL could POST forged payloads and have us deliver OTPs through our Meta quota — both an abuse vector and a quota-drain. The verifier uses `crypto.timingSafeEqual` (timing attack resistant) and a 5-minute timestamp skew window (bounds replay attacks). Multiple `v1,<sig>` entries in the header are tolerated so Supabase secret rotation works without an outage window — same defence-in-depth posture as the Razorpay HMAC discussion in slice 5.
  - **Fail-loud-not-silent on missing env.** `readConfig()` collects every missing var and the route returns HTTP 500 with a generic-to-client message; the specific missing var is server-logged only. A previous draft considered fallback-to-test-OTP behaviour and rejected it — silently succeeding when Meta is down would let members appear-signed-in without ever receiving a code. The Supabase-side `[auth.sms.test_otp]` map is the explicit dev path; this hook never short-circuits.
  - **Meta failures translate to generic-to-client.** When Meta returns 4xx/5xx, the route logs the full response body server-side (capped at 500 chars) and returns "Could not send OTP. Please try again." to the client. Meta error codes are never surfaced to the member.
  - **Raw-body discipline.** The HMAC is computed over the raw request body — the route uses `request.text()` and parses JSON AFTER signature verification. JSON-parse-then-restringify would change whitespace and break the signature check; we avoid that footgun. The signed payload format `${id}.${ts}.${body}` matches the Standard Webhooks v1 spec.
  - **OTP is never logged.** Only failure reasons + counts make it to console; the OTP value, the access token, and the Graph API path are excluded by design. Documented in WHATSAPP.md "Never log the OTP" so future logging additions inherit the same rule.
  - **Test phones bypass the hook.** `supabase/config.toml [auth.sms.test_otp]` declares three dev phones that Supabase short-circuits before the SMS provider — so UI work continues without burning Meta send quota. Hook only fires for real numbers.
  - **No DB writes from the route.** Pure passthrough hook (read OTP from payload → forward to Meta). No service-role client, no `members` mutation, no audit table — the auth bootstrap trigger from migration 0014 handles member-row creation on successful verify, not on send.
  - **CLAUDE.md document map updated** with the new WHATSAPP.md so future sessions can find the Meta ops doc.
  - **End-to-end test is DEFERRED** until Meta WABA + Authentication template are approved (per WHATSAPP.md Phases A–C; founder action). Code is built behind a placeholder set of env vars. AGENDA §2 "Meta WhatsApp OTP via Supabase Auth Hook" stays UNCHECKED until live delivery is verified — ticking it now would falsely signal a working end-to-end path.
- 2026-05-27 — Auth — Phase 1 §2 slice 0 (consent capture at signup, DB side) applied to remote. Migration `20260527190000_consent_at_signup.sql`. Adds `members.terms_accepted_at timestamptz` + `members.terms_version text` (partial index on the latter for "who accepted version X" admin lookups). Rewrites `handle_new_auth_user()` (the post-OTP auth bootstrap trigger from slice 2) so it reads `raw_user_meta_data` set by the client at `signInWithOtp({ options: { data: { … } } })` time — consent persists through the OTP round-trip without an extra client write. Strict-typed extraction with try/catch around each cast: a malformed consent payload defaults to NULL rather than failing auth (sign-in must not break because the client sent garbage). Trigger keeps slice 2's `security definer + set search_path = public + on conflict (id) do nothing`. — **Security & connector checks:**
  - **Field whitelist enforced inside the trigger** — the trigger reads ONLY `opt_in_whatsapp`, `terms_accepted_at`, `terms_version` from `raw_user_meta_data`. `role`, `trust_level`, `id_verification`, `recognised_surname`, `phone`, `full_name`, `surname`, etc. are silently ignored. A client cannot use the metadata channel as a back door to seed admin / trust fields at signup. Plain enumeration of the three accepted keys (not a generic spread) — review-obvious.
  - **Consent columns are write-once at the DB layer.** Slice 2's profile-fields UPDATE column grant on `members` re-issued with `terms_accepted_at` + `terms_version` INTENTIONALLY excluded (also noted in an in-grant comment so the exclusion isn't an accident). Members cannot rewrite their own consent record post-signup. Re-acceptance after a Terms version bump is a future admin/system path — deferred until the first version bump actually happens.
  - **Re-grant uses `revoke update … from authenticated; grant update (…columns…) …` in place** — same whitelist as slice 2 plus the explicit comment. No policy gap window (column grants are not policies; the GRANT statement is atomic at commit).
  - **Trigger error path is "default to NULL, log nothing"** — auth must not fail on malformed metadata. Once the UI is stable I'll add a `raise notice` for observability, but for now silent fallback is correct (members do not benefit from seeing "your consent payload was malformed" — they need to be signed in).
  - **`TERMS_VERSION = "v1.0"` lives in `packages/shared/src/consent.ts`** as the single source of truth. Bumping the constant is the trigger for a future re-acceptance flow; nothing in DB enforces equality between the constant and stored value (intentional — historical rows record what was accepted at the time).
  - **End-to-end trigger test is DEFERRED** until phone OTP is wired to remote. `supabase/config.toml`'s `[auth.sms]` block (`enable_signup = true`, `test_otp` dev numbers, 30-day rolling `inactivity_timeout`) is local-CLI-only — remote phone provider settings live in the Supabase dashboard, and the Meta WhatsApp Cloud API Authentication-template + Auth Hook are a separate slice. Column existence verified via anon REST: `select=terms_accepted_at,terms_version,opt_in_whatsapp` returns HTTP 200 + `[]` (would 400 with `42703` if columns were missing). Migration list confirms 14 / 14 registered on remote. The trigger's metadata-harvest path will be exercised end-to-end when the OTP flow lands; until then the DB plumbing is structurally correct but not behaviourally validated.
- 2026-05-27 — Data — Phase 1 §1 final slice (Community Intelligence aggregate views) applied to remote. Migration `20260527180000_community_intelligence_views.sql`. 5 views shipped — `members_directory` (backfills the slice-2-deferred `members_public` slot; safe-column whitelist over `members`; derives `age_band` from DOB so raw DOB stays hidden), `member_profession_directory` (one row per (member, profession), joined to cities + sub_communities + specialties — powers SPEC §3 drill-down country → state → city → PIN → profession → specialty + status + gender + age_band + sub-community + trust_level), `member_capability_directory` (one row per (member, capability) joined to cities — the actionable "who is reachable for help" layer per SPEC §3), `community_pulse` (single-row, 12-column public landing-page state + proxy counts per SPEC §3.1), `community_pulse_by_profession` (per-profession breakdown for the "Total Professionals (by category)" line, ordered most-to-least). Closes Phase 1 §1 Data model entirely. — **Security & connector checks:**
  - **All 5 views are intentionally definer views (`security_invoker = false`).** `members` RLS is SELECT-own + admin; the Directory + Pulse genuinely need cross-RLS reads. Postgres has no column-level RLS, so the privacy boundary IS the column whitelist baked into each view (excludes phone, email, raw DOB, opt-in flags). Documented loudly at the top of the migration so reviewers don't read the bypass as accidental. Supabase Security Advisor will surface a "security definer view" warning on these — that's the intended signal; the user should acknowledge it in the dashboard rather than silence the advisor globally.
  - **Anon REST smoke-tested**: `community_pulse` + `community_pulse_by_profession` return HTTP 200 with the expected shape (all counts 0 because no members yet — verifies the schema works, not the data). `members_directory` + `member_profession_directory` return HTTP 401 + `code=42501 "permission denied for view"` — anon is correctly denied via `revoke select … from anon`. Identical posture to the payments table.
  - **Connector-safe wording in column names**: `total_connections_facilitated`, `total_stays_facilitated`, `total_rides_facilitated` — never "booked", never money. Matches SPEC §3.1 wording rule. Activity counts use review-as-proxy + connected-inquiry-as-proxy, never inferred from any money signal.
  - **Per-metric threshold guard is app-side for now.** SPEC §3.1 says "a counter only goes live once it crosses a minimum (config per metric)" — that admin config table is part of §9 Launch polish (data-gated) and not built yet. View columns are RAW counts; the landing-page client decides whether each one is ready to show. Recorded for §9.
  - **Chained-view discipline**: `member_profession_directory` and `member_capability_directory` read FROM `members_directory` (not directly from `members`) so the safe-column whitelist + age_band derivation stay in one place. DRY for maintenance — changing the privacy boundary or band breakpoints means editing one view.
  - **No new indexes added** — the views compose existing FK + status indexes. For Phase 1 community size (hundreds of members) the queries are sub-millisecond. SPEC §3 calls for materialization "if counts grow large" — promote each to MATERIALIZED VIEW with a pg_cron refresh job in §9 once a real perf signal appears. Recorded for §9.
  - Migration list confirms 13 / 13 (lookups ×3 + core ×2 + trust ×2 + magazine ×2 + payments ×2 + perf-wrap ×1 + community-intel ×1) registered on remote.
- 2026-05-27 — Data — Phase 1 §1 audit pass for "RLS policies on every table" — verified + applied performance wrap. Migration `20260527170000_rls_perf_wrap.sql` rewrites every RLS policy across slices 2–5 to use the Supabase-recommended `(select auth.uid())` / `(select public.is_admin())` / `(select public.is_editor())` subquery pattern instead of bare per-row function calls. The Supabase Postgres best-practices skill (now installed at repo root `.agents/skills/supabase-postgres-best-practices/`, ref `references/security-rls-performance.md`) flags this as a CRITICAL-priority rule (5–10× faster on moderate tables, 100×+ on 1M-row tables). 39 policies altered across 14 tables; behaviour is identical, only the query plan changes. ALTER POLICY used in place (no DROP + CREATE, no policy-gap window). — **Audit findings recorded:**
  - **20 / 20 tables have RLS enabled** (lookups ×6, core ×4, trust ×7, magazine ×2, payments ×1). Verified via grep over `enable row level security` statements vs. `create table public.` statements in the migrations.
  - **Every table has at least one policy OR is service-role-only by design**. Payments has SELECT-own (member) + SELECT-all (admin) and zero write policies — the only-writer is the future Razorpay server flow under service_role; this is deliberate, documented in slice 5's audit entry.
  - **Lookup tables (×6)** use `using (true)`; no auth.uid() to wrap. No change needed.
  - **Helper functions `public.is_admin()` and `public.is_editor()`** are STABLE + SECURITY DEFINER + parameterless → identical wrap pattern applied. Their per-row evaluation was the second hot path in policies that combine identity + admin checks.
  - **Indexes on RLS-comparison columns** verified — `members(id)` is the PK; `member_professions.member_id`, `member_capabilities.member_id`, `verifications.member_id`, `listings.member_id`, `inquiries.seeker_id`, `saved_alerts.member_id`, `reviews.author_id`, `reports.reporter_id`, `requests.member_id`, `submissions.author_id`, `payments.member_id` all have explicit indexes from their slice migrations. The skill's second-half guidance ("always add indexes on columns used in RLS policies") is already met.
  - **The `members_public` cross-member view is still deferred** to §3 Directory slice (noted in slice 2 audit). No change here — that's a structural deferral, not an RLS gap.
  - **The vouch-flow INSERT on `verifications`** remains deliberately unimplemented (noted in slice 2 audit) — ships with the §2 Auth verification UI when the voucher-initiated path is built.
  - **Cities INSERT policy** for the "suggest a city" UI remains deferred to §2 Auth profile (noted in slice 0c audit). No gap — writes denied for now is the correct posture until the UI exists.
  - Saved this rule as a feedback memory so future slices ship in the wrapped form from the first draft.
  - Migration list confirms 12 / 12 (lookups ×3 + core ×2 + trust ×2 + magazine ×2 + payments ×2 + perf-wrap ×1) registered on remote.
- 2026-05-27 — Data — Phase 1 §1 slice 5 (payments schema + RLS) applied to remote. Migrations `20260527160000_payments_schema.sql` + `20260527160100_payments_rls.sql` ran cleanly via `supabase db push`. 1 new table — payments (the ONLY money-handling table in Nagarsetu; listing-fee only). State machine `created → paid | failed | refunded` with CHECK constraints tying `paid_at` + `gateway_payment_id` to `status in ('paid','refunded')` and `refunded_at` to `status='refunded'`. Snapshot fields `amount` (paise) + `currency` (default 'INR', single-value CHECK) + `term_days_paid` (1–365) lock the receipt against future admin price changes — same discipline as the existing `listings.price_paid` / `term_days_paid` from slice 3. SPEC's single `gateway_ref` field split into `gateway_order_id` (Razorpay `order_xxxxx`, NOT NULL + UNIQUE) + `gateway_payment_id` (`pay_xxxxx`, UNIQUE, set on capture) + `gateway_signature` (HMAC verified server-side) — required by Razorpay's two-phase flow. 5 indexes including a partial `(listing_id, paid_at desc) where status='paid'` for the renewal-flow lookup. — **Security & connector checks:**
  - **Hard constraints #1–#4 enforced at the SCHEMA layer, not just RLS**: `listing_id NOT NULL` + a BEFORE INSERT/UPDATE trigger `enforce_payment_payer_is_listing_owner()` asserts `payments.member_id = listings.member_id`. Together these structurally guarantee the table cannot record a member-to-member transfer — only "listing owner X paid the listing fee for their listing Y". Holds even if a future bug, fixture, or admin script bypasses RLS. Trigger function pins `search_path = public` (no SECURITY DEFINER — only service_role writes payments, so privilege elevation would buy nothing and add surface).
  - **No member-facing INSERT / UPDATE / DELETE policy exists.** The ONLY writer is the server-verified Razorpay flow under `service_role`. This is a deliberate structural defense of the AUDIT checklist item "Payment verified server-side against the gateway (never trust client success)" — a client cannot synthesise a `paid` row even if it knew the table name.
  - **Defense in depth grants**: `revoke insert, update, delete on payments from authenticated, anon` (so even an upstream RLS misconfig can't permit a client write). `revoke select on payments from anon` (anon shouldn't see payments exist at all).
  - **Anon REST smoke-tested**: returns HTTP 401 + `code=42501 "permission denied for table payments"` — exactly the desired stricter posture vs. earlier tables' "HTTP 200 + 0 rows" (those still grant SELECT, just policy-filter to empty).
  - **`on delete restrict`** on both FKs (`member_id`, `listing_id`) — a member or listing with payment history cannot be cascade-deleted. Payment history is part of the connector audit trail (Razorpay receipts must outlive listing status changes). Member account deletion will need an explicit admin path that anonymises or migrates payment rows; recorded for §8 Hardening.
  - **Pricing/settings config table NOT in this slice** — it's a separate AGENDA §4 item ("Admin pricing/settings"). This slice only handles the payment record; the server flow will read whatever current price is configured and pass it as the `amount`/`term_days_paid` snapshot at INSERT.
  - **Currency CHECK whitelists 'INR' only** (matches Open Decision #7 working assumption: INR globally for Phase 1 via Razorpay international cards). Widening is a CHECK list change here, not a schema migration.
  - Migration list confirms 11/11 (lookups schema/seed/rls + core schema/rls + trust schema/rls + magazine schema/rls + payments schema/rls) registered on remote.
- 2026-05-27 — Data — Phase 1 §1 slice 4 (magazine schema + RLS) applied to remote. Migrations `20260527150000_magazine_schema.sql` + `20260527150100_magazine_rls.sql` ran cleanly via `supabase db push`. 2 new tables — magazine_issues (lifecycle open→curating→rendered→published; CHECK enforces rendered/published needs `rendered_pdf_url` and published needs `publish_date`; partial index on `publish_date desc where status='published'` for the latest-cover query), submissions (issue_id nullable until editor approves+assigns; status submitted→approved|rejected; CHECK enforces decision rows have reviewer + timestamp and only approved submissions can hold an issue_id; FTS GIN on title+body via simple config; partial submitted-inbox index). New helper `is_editor()` (role IN ('editor','admin'), SECURITY DEFINER + `set search_path = public`, same recursion-break trick as `is_admin`). — **Security & connector checks:**
  - **magazine_issues SELECT `status='published'` is open to anon** for the AGENDA §3 guest-browse landing magazine-cover teaser; all other statuses gated to editor + admin. Explicit `grant select on public.magazine_issues to anon`. CRUD editor + admin only.
  - **submissions individual rows are private** — author + editor + admin only. No broad authenticated read; the rendered PDF is the canonical reader surface per SPEC §6, and the public "Articles Published" counter will come from a SECURITY DEFINER aggregate view in slice 6 (not direct table access).
  - **Authors can't promote their own work**: INSERT pinned to `status='submitted'` with all decision fields NULL; UPDATE allowed only while still `'submitted'` and the WITH CHECK re-pins those decision fields. Column INSERT/UPDATE grants further block status, issue_id, pull_quote, reviewed_by, reviewed_at, review_notes at the grant level (defence in depth).
  - **DELETE bounded**: author may withdraw only while still `'submitted'`; editor + admin can delete any.
  - **city_text snapshotted at submission time** (free text, not a live FK to cities) — the byline shouldn't drift if the author later moves; matches the price-snapshot discipline from §7.1.
  - **Anon REST smoke-tested**: HTTP 200 + 0 rows on both tables via `apikey:` + `Authorization: Bearer` with the publishable key — magazine_issues correctly returns no rows because no `published` issue exists yet; submissions correctly returns no rows because no policy matches anon.
  - Migration list confirms 9/9 (lookups schema/seed/rls + core schema/rls + trust schema/rls + magazine schema/rls) registered on remote.
- 2026-05-26 — Policy — Locked **session & device policy** (SPEC §1.5, AGENDA §2, MEMORY). 30-day rolling refresh-token window per device; multi-device unlimited; short access token (1 h) auto-refreshed by SDK in background — no OTP re-prompt during the window. Driver: cut Meta WhatsApp OTP delivery cost (~₹0.30–₹0.50/send; ~4× annual saving for an active member) by stretching practical login retention. Explicitly **not** long-lived JWT — short access + long rolling refresh preserves revocation. — **Security checks:** refresh tokens stored in platform-secure storage only (httpOnly secure cookies on web via Supabase SSR; expo-secure-store → iOS Keychain / Android Keystore on mobile — never AsyncStorage / localStorage); **active sessions UI mandatory before launch** so members can self-serve per-device sign-out + "sign out everywhere"; admin-triggered revoke-all path for account-takeover response; this dovetails with the existing checklist items "Phone OTP rate-limited", "Session handling correct on web + mobile; no token leakage", and "Role checks enforced server-side, never trusted from client" — no new gaps opened. Connector-integrity unaffected (auth is not a money path).
- 2026-05-27 — **SECURITY INCIDENT** — Real Supabase **legacy `service_role` JWT** plus **legacy anon JWT** were committed in `.env.example` (commit `63ed65c` from 2026-05-26 — I made that commit editing one line of the file without reviewing the rest). Repo is public; keys were readable in git history. **Resolution:** founder disabled JWT-based legacy keys in Supabase dashboard at `2026-05-27T07:51:47Z` (verified via REST: legacy JWT now returns HTTP 401 *"Legacy API keys are disabled"* — leaked key is inert). Replaced committed `.env.example` with all-placeholder version (sb_publishable_/sb_secret_ format only); removed redundant `env.example` (no leading dot). Added explicit ⚠ rotation note to template header. **Git history retains the now-dead JWTs** — chose rotation-makes-inert over BFG rewrite + force-push (less destructive, equivalent security outcome). **No evidence of exploitation observed.** — Lessons captured: any commit touching a secrets-shaped file MUST trigger a full-file diff review, not just an edit. Saved as a feedback memory.
- 2026-05-27 — Data — Phase 1 §1 slice 3 (trust economy schema + RLS) applied to remote. Migrations `20260527140000_trust_economy_schema.sql` + `20260527140100_trust_economy_rls.sql`. 7 new tables — listings (with §7.1 paid-listing lifecycle fields published_at/expires_at/price_paid/term_days_paid baked in + FTS GIN index on title+description via simple config + 6 indexes incl. partial on active+expires), availability (indicative date windows; check start≤end), inquiries (seeker_id + listing_id + nullable dates/qty per time_binding + channel enum), saved_alerts (nulls = match-all per axis; unique tuple prevents dupes), reviews (listing-tied or general; check author≠subject), reports (nullable listing_id; check reporter≠subject; check actioned/dismissed rows must have reviewer), requests (FREE demand-side posts; gender_pref + food_pref for PG). — **Security & connector checks:**
  - **Listings SELECT is status-aware**: active visible to all authenticated; draft/paused/expired only to owner + admin. Connector ethos preserved (no surprises about visibility).
  - **System-managed columns on listings (fee_paid, published_at, expires_at, price_paid, term_days_paid, view_count) blocked at column-grant level**. Payment flow + cron use service_role. Members can never self-claim a paid status.
  - **Availability & inquiries use EXISTS subqueries** referencing listings — visibility correctly cascades from parent listing status. Acceptable performance for Phase 1; revisit at scale.
  - **Inquiries can be `closed` but never `DELETE`d** — lead history is part of the connector audit trail (per "App is never a party to disputes" we keep the record, even if neither party acts on it).
  - **Reports SELECT: reporter + admin only**. Subject of report does NOT see it — DISPUTE.md flow surfaces admin actions separately. Prevents retaliation. Report INSERT decision-fields (status / action_taken / reviewed_by / reviewed_at) blocked via column grants. No DELETE (moderation audit).
  - **Reviews enforce author ≠ subject at DB level** (CHECK constraint).
  - **Anon REST smoke-tested**: HTTP 200, 0 rows on all 7 tables.
  - Migration list confirms 7/7 registered on remote.
- 2026-05-27 — Data — Phase 1 §1 slice 2 (core schema + RLS) applied to remote. Migrations `20260527100000_core_schema.sql` + `20260527100100_core_rls.sql` ran cleanly. 4 new tables: `members` (1:1 with auth.users via FK on id ON DELETE CASCADE; profile fields nullable until filled), `member_professions` (with new `expertise_text` ≤280 chars + FTS index using to_tsvector('simple', …) for Gujarati+English search + `years_experience` smallint 0-99 + cascading profession/specialty FKs), `member_capabilities` (kind enum + domain + is_offered with partial index), `verifications` (history; CHECK enforces approved/rejected rows record reviewer). Helpers: `set_updated_at()` reused from slice 1, `is_admin()` SECURITY DEFINER (breaks RLS recursion), `is_profile_complete(uuid)`. Auth bootstrap trigger `on_auth_user_created` → `handle_new_auth_user()` (SECURITY DEFINER, search_path=public) creates a minimal (id, phone) members row on OTP success, idempotent via ON CONFLICT. — **Security & connector checks:**
  - **members RLS restrictive by design**: SELECT own row + admin sees all only. No INSERT (trigger handles), no DELETE (cascade from auth.users handles). Column-level UPDATE grants block members from updating role, trust_level, id_verification, recognised_surname, phone — those are admin/service-role only.
  - **member_professions + member_capabilities**: cross-member SELECT for authenticated (powers directory + FTS + Community Intelligence drill-down); INSERT/UPDATE/DELETE confined to own rows. `is_verified` on member_professions is column-grant-protected (admin-set credential mark, members can't self-promote).
  - **verifications**: SELECT own + admin. INSERT restricted to method ∈ {'referral','document'} with status='pending' and no reviewer set (members can't self-decide). UPDATE admin-only. No DELETE (audit retained). Column INSERT grant blocks status/reviewer fields.
  - **Vouch flow INSERT is intentionally not yet allowed** — a member shouldn't be able to claim someone vouched for them without the voucher's consent. The voucher-initiated INSERT policy ships when the §2 Auth verification UI is built (will require `auth.uid() = voucher_member_id`).
  - **`members_public` view deferred to §3 Directory** — until that view is built, no UI can cross-read members. Explicit limit. Prevents accidental phone/email/DOB leakage via a careless SELECT.
  - **Anon REST smoke-tested**: HTTP 200 + 0 rows on all 4 tables, confirming the publishable key + anon role can connect but RLS correctly returns no data.
  - Migration list confirms 5/5 (lookups schema/seed/rls + core schema/rls) registered on remote.
- 2026-05-26 — Data — Migration 0003 (`20260526160200_lookups_rls.sql`) applied to remote: RLS enabled on all 6 lookup tables; `SELECT using (true)` policy on each; no INSERT/UPDATE/DELETE policies (those operations now DENIED for anon + authenticated; service_role still bypasses RLS for admin code). Closes the slice-1 exposure window where the anon key could write any lookup row. — Checks: SELECT-public is correct (lookups are public reference data — autocomplete, directory filters, magazine genres); writes now admin-only; the cities INSERT policy for the suggest-a-city UI deferred until that UI lands in §2 Auth (will allow `authenticated` to insert with `status='pending'` only). NOTICE on apply: the long policy name on cities was truncated to 63 chars — Postgres identifier limit, policy functions correctly under the truncated name. Overall AGENDA "RLS policies on every table" remains unchecked (lookups done; core / trust-economy / magazine / money still pending).
- 2026-05-26 — Data — Phase 1 §1 slice 1 (lookups schema + seeds) applied to live Supabase project `wfkeyjukkoktubeqvjch` (Tokyo). Migrations `20260526160000_lookups_schema.sql` + `20260526160100_lookups_seed.sql` ran cleanly via `supabase db push`. 6 tables created: cities (64 seeded), sub_communities (6, Gujarati), professions (30), specialties (53 — Doctor 20 / Engineer 9 / Software 10 / Lawyer 7 / CA 4 / Teacher 3), listing_categories (9, with time_binding enum), genres (10, Setusarjan list in Gujarati). `cities.status` + `cities.merged_into` support the suggest-and-approve flow per the new MEMORY locked decision. — **Security: ⚠ RLS NOT YET ENABLED on the 6 tables.** Default Supabase grants mean the `anon` role can currently INSERT / UPDATE / DELETE any lookup row using the public anon key. SELECT-only would be defensible (lookups are public data), but unrestricted writes are a real exposure for spam (fake cities, fake professions). Recommend a thin follow-up migration to enable RLS + add SELECT-everyone / no-write policies *before* any client code with the anon key starts hitting these tables. Otherwise scheduled to land in slice 2 alongside the core tables' RLS pass.
- 2026-05-26 — Feature — Fleshed out **Education pillar** (SPEC §7.2 / AGENDA §7b) to match CEA depth. SPEC §2 gained a new Education sub-section with `scholarships` + `scholarship_applications` + `scholarship_alerts` tables (previously the schemas lived only inline inside §7.2). `reports` extended with nullable `scholarship_id`. SPEC §7.2 expanded into four labelled sub-flows: 7.2.1 Scholarships (L2 verification gate to publish · publish flow · fan-out with channel discipline · application state machine · lifecycle cron · connector disclaimer · reactive moderation · document handling), 7.2.2 Career guidance (reuses `member_capabilities` + mentorship), 7.2.3 Education listings (uses the `education` listing category — paid like Business), 7.2.4 Student profiles (`status = studying` recipients). AGENDA §7b expanded from 4 to ~14 bullets with sub-headings. MEMORY locked the Education pillar policy alongside the existing context note. — **Security & connector checks:**
  - **L2 ID verification enforced server-side** before any `scholarships` row can be inserted by a member (API check; never trust client).
  - **Offering member is the sole adjudicator** — schema constraint: `scholarship_applications.decided_by_member_id` must equal `scholarships.offered_by_member_id`; admin overrides go through a separate, audit-logged moderation path.
  - **Application documents** stored in a scoped Storage bucket; RLS scoping so only the applicant + the offering member + admins can read; type/size checks on upload.
  - **Reactive moderation path wired on day 1** — `reports.scholarship_id` exists from launch; admin actions follow `DISPUTE.md`.
  - **WhatsApp opt-in respected** — Utility template + matching `scholarship_alerts` opt-in required; broader fan-out is in-app + email only; never blast WhatsApp to non-opted members (protects WABA quality).
  - **Connector disclaimer mandatory** on every scholarship card + notification body (offering party named; Nagarsetu is the bridge, not the adjudicator).
  - **No money flow through the app** — scholarships are free both sides; the only Education paid surface is the `education` listing category (handled by §7.1 mechanisms).
  - **Gujarati typesetting discipline** applies to title/description rendering.
- 2026-05-26 — Feature — Added **Community Event Announcements** (broadcast, no admin pre-approval). SPEC §2 gained `community_events` + `community_event_alerts` tables and added `community_event_id (nullable)` to `reports`. SPEC §7.3 fully fleshed out (publish flow, notification fan-out, lifecycle, reactive moderation, WhatsApp template requirements). AGENDA §7c expanded from 3 placeholder bullets into the full buildable set. MEMORY locked the policy alongside the existing context note. — **Security & connector checks:** rate-limit enforced server-side (count member's announcements in trailing `community_event_rate_limit_days` window; reject at the API, never trust client); `significance_confirmed_at` is required on insert (DB-level NOT NULL); reactive moderation path wired on day 1 (`reports.community_event_id`), not retrofitted; WhatsApp opt-in respected (proactive blasts only to `opt_in_whatsapp = true`; pre-approved Marketing template required before broadcast channel can go live; non-opted members get in-app + email only); connector disclaimer mandatory on every announcement card + notification body (organising body + creator-supplied contact named; Nagarsetu is the bridge, not the organiser); RLS — `community_events` writable only by `created_by` (and admins for moderation); `community_event_alerts` writable only by owning member; Gujarati typesetting discipline applies to title/description rendering.
- 2026-05-26 — Scope — Resolved Open Decision #5 (Phase-1 cut, full): promoted four more Phase 2 items to Phase 1 — Matrimony (AGENDA §3a, SPEC matrimony_profiles + matrimony_interests), Mentorship matching (§3b, mentor_offers + mentee_requests + mentorships), Member blogs (§3c, blogs), Magazine audio + searchable archive (§6a, submission_audio + Postgres FTS). Heritage authorship locked: **member-contributed with admin review** (AGENDA §7, SPEC heritage_articles schema finalised). Phase 2 trimmed to a single deferred item — optional in-app member-to-member payments, explicitly held back to protect Hard Constraint #1 (logged in MEMORY as a re-affirmation; promotion requires an explicit override). — Checks: connector ethos preserved (no money flows added); matrimony has restricted visibility by default (privacy-first); all new features have a connector disclaimer requirement; scope expansion noted — solo-dev Phase 1 just roughly doubled in size, plan accordingly.
- 2026-05-26 — Scope — Re-scoped Open Decision #5 (Phase-1 cut, partial): **Events & heritage modules moved from Phase 2 → Phase 1.** New AGENDA §7 with bullets for events table + RSVPs + feed integration + heritage articles (schema sketched in SPEC §2 Community, full authorship policy TBD before build). Renumbered AGENDA §7-9 → §8-10; updated MEMORY ref to §9 Hardening. Phase 2 list trimmed (events, event_rsvps removed; matrimony/mentorship/blogs/audio-magazine/optional-payments still Phase 2). — Checks: connector disclaimer required on event surfaces; events are member-organised, not Nagarsetu-organised.
- 2026-05-26 — Policy — Partially resolved Open Decision #6 (Governance): user-facing label "Nagarsetu Admins" applies to both Editor and Admin roles everywhere — UI copy, disclaimers, magazine credits, notifications. Schema's `members.role (member|editor|admin)` enum stays for technical routing of magazine vs moderation flows. Specific powers per role + assignment of first humans deferred to launch hardening (added to `AGENDA.md` §8). Gujarati equivalent of label TBD at i18n. — Checks: connector ethos preserved (no escalation of admin power claims); deferral is explicit, not silent.
- 2026-05-26 — Policy — Resolved Open Decision #4 (Geography): global from day one — Nagars everywhere; diaspora is Phase 1, not "expand later". Added `SPEC.md` §1.5 (Geography & diaspora) with examples (Tokyo welcome / Utah PG / Nairobi Masai Mara assistance); added `members.timezone` (IANA) for member-local reminders; updated AGENDA seed-cities task to "global from day one" and ID-verification task to accept any government-issued ID. New Open Decision #7 added: listing fee currency for international Business listings (working assumption: INR globally for Phase 1 via Razorpay international cards). — Checks: connector ethos preserved cross-border (offline deals only); ID-verification scope-creep avoided (still only required for L2 hosting/rentals, not for joining).
- 2026-05-26 — Policy — Resolved Open Decision #3 (Free/paid boundary): only Business listings are paid; every other category (Room/Vehicle/PG/Goods/Tour/Service) and all Requests are free. Free listings get a soft expiry with one-tap refresh (default 60–90 days, admin-configurable). Propagated through `MEMORY.md`, `SPEC.md` (added §7.1a Free listing lifecycle; added `is_paid` to listing_categories), and `AGENDA.md`. — Checks: no schema split (same `listings` table for both lifecycles); pricing/settings flow unchanged for Business; connector philosophy preserved (free for community sharing, paid only for commercial reach).
- 2026-05-26 — Foundations — GitHub repo initialised at github.com/jmbuch19/nagarsetu; all 8 root docs committed (`CLAUDE.md`, `CONCEPT.md`, `SPEC.md`, `AGENDA.md`, `MEMORY.md`, `AUDIT.md`, `DISPUTE.md`, `IDEAS.md`) plus `.env.example` (placeholders only — no real secrets) and `.gitignore`. Supabase CLI installed via Scoop; `supabase init` ran; project linked to ref `wfkeyjukkoktubeqvjch` (Tokyo region) under account `jambuch@gmail.com`. — Checks: verified `.env.example` contains no live credentials before pushing; fixed a `.gitignore` bug where an inline comment on `!.env.example` would have caused the template to be ignored; `supabase/.gitignore` ignores `.temp/` and `.env*.local` so project-ref + local secrets stay out of git.
- _YYYY-MM-DD_ — _area_ — _what was built_ — _security items verified_
